if __name__ == '__main__':
    a = int(input())
    b = int(input())

    # Calculation of the sum, difference and product of the two numbers
    sum = a + b
    diff = a - b
    prod = a * b

    # Printing the results as per the requirement
    print(sum)
    print(diff)
    print(prod)
